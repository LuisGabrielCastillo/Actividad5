# List of tuples
# Each tuple contains a test: the first element are the inputs, the second are the output, and the third is the message in case of an error
# To test another case, add another tuple

input_values = [
    (
        # Inputs
        ["4", "34"],
        # Outputs
        ["", "","5.0"],
        # Error message
        "Revisa los acentos. Revisa la prioridad de operadores. Revisa el tipo de datos"
    ),
    (
       # Inputs
        ["6", "8"],
        # Outputs
        ["", "","10.0"],
        # Error message
        "Revisa los acentos. Revisa la prioridad de operadores. Revisa el tipo de datos"
    ),
    (
        # Inputs
        ["12", "316"],
        # Outputs
        ["", "","20.0"],
        # Error message
        "Revisa los acentos. Revisa la prioridad de operadores. Revisa el tipo de datos"
    )
]